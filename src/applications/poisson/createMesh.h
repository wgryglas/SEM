/*

cout<<info<<"Creating mesh ...."<<endl;

IOFile meshFile(rootCase+"mesh.sem");
IOFile elementsFile(rootCase+"elements.sem");

Mesh mesh = Mesh
(
 meshFile,
 elementsFile
);

mesh.writeSpectralNodes();

cout<<endInfo;

*/
